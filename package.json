{
  "name": "echo-of-baduga",
  "version": "1.0.0",
  "description": "Echo of Baduga - Stream Baduga Music & Culture",
  "main": "index.html",
  "scripts": {
    "init": "npm install @capacitor/core @capacitor/cli && npm install @capacitor/android && npx cap init \"Echo of Baduga\" \"com.echobaduga.app\" --web-dir=www",
    "build": "powershell -ExecutionPolicy Bypass -File build_android.ps1",
    "sync": "npx cap sync",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/ios": "^5.7.0",
    "jsdom": "^29.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
